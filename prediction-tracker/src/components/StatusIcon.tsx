import React from 'react';

export interface StatusIconProps {
  status: string;
}

export const StatusIcon: React.FC<StatusIconProps> = ({ status }) => {
  // TODO: map status to icons and colors
  return null;
};